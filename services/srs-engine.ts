export function review(c:{interval:number;ease:number;repetitions:number;lapses:number},q:number){q=Math.max(0,Math.min(5,Math.round(q)));let e=Math.max(1.3,c.ease+.1-(5-q)*(.08+(5-q)*.02),i=c.interval,r=c.repetitions,l=c.lapses;if(q<3){r=0;l++;i=1}else{r++;i=r===1?1:r===2?6:Math.max(1,i*e)}return{interval:i,ease:+e.toFixed(3),repetitions:r,lapses:l,dueAt:new Date(Date.now()+i*86400000).toISOString()}}